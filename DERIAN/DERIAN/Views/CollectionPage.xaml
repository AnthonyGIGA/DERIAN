<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:DERIAN="clr-namespace:DERIAN" xmlns:PanCake="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView" xmlns:controls="clr-namespace:DERIAN.Controls"
             mc:Ignorable="d"
             x:Class="DERIAN.Views.CollectionPage">
    
    <ContentPage.Resources>
        <Style TargetType="StackLayout">
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="Selected">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="White" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
    </ContentPage.Resources>

    <ContentPage.Content>

        <StackLayout Margin="15,25,25,0">
                 <controls:AdControlView Grid.Row="1"
                                HeightRequest="50"
                                AdUnitId="{Static DERIAN:Constants.AdUnitIdTest}"
                                HorizontalOptions="FillAndExpand" />
             <StackLayout Spacing="0"
                     Margin="{OnPlatform Android='10, 10, 0, 0', iOS='0'}">
                <Label 
                   x:Name="titulocolle"
                   FontSize="Title"
                   TextColor="{DynamicResource mainLabel}" 
                   VerticalOptions="CenterAndExpand"
                   HorizontalOptions="StartAndExpand"/>


                <BoxView 
                     VerticalOptions="End"/>

                <CollectionView
                    x:Name="ListaItems"
                    SelectionMode="Single" 
                    SelectionChanged="AbrirItem"
                    >

                    
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout  Margin="0" VerticalOptions="CenterAndExpand">
                                     
                                     <Label
                                                        FontSize="20"
                                                        HorizontalOptions="Center"
                                                        Text="{Binding nombre}"
                                                        TextColor="Black"
                                                        Margin="0,25,0,0"
                                                        VerticalOptions="Start">

                                     </Label>
  
                                    <Frame
                                                        CornerRadius="5"
                                                        HasShadow="False"
                                                        VerticalOptions="Center"
                                                        IsClippedToBounds="True">
                                        <Image
                                                            Aspect="AspectFit"
                                                            HeightRequest="150"
                                                            Source="{Binding imagen}"
                                                            WidthRequest="75" />
                                    </Frame>
                                  
                                         
                            </StackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout ItemSpacing="0" Orientation="Vertical" />
                    </CollectionView.ItemsLayout>

                </CollectionView>
            </StackLayout>
 
            <PanCake:PancakeView Grid.Row="2" Grid.Column="1" BackgroundGradientStartColor="#6988f5" BackgroundGradientEndColor="#55a0f3" CornerRadius="28" HorizontalOptions="CenterAndExpand" Margin="0,0,0,0">
                <Button x:Name="botonAgregarItem" Clicked="click_agregar"  Text="Agregar Ítem" FontSize="20" FontAttributes="Bold" BackgroundColor="Transparent" HeightRequest="55" TextColor="White" WidthRequest="300"/>
            </PanCake:PancakeView>
            <PanCake:PancakeView Grid.Row="3" Grid.Column="1"  BackgroundGradientStartColor="#6988f5" BackgroundGradientEndColor="#55a0f3" CornerRadius="28" HorizontalOptions="CenterAndExpand" >
                <Button x:Name="botonAgregarCampoCustom" Clicked="agregar_campo_custom"  Text="Lista Campos Custom" FontSize="20" FontAttributes="Bold" BackgroundColor="Transparent" HeightRequest="55" TextColor="White" WidthRequest="300"/>
            </PanCake:PancakeView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>